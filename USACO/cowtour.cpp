/*
ID: blitesa1
LANG: C++14
PROG: cowtour
*/

#include <bits/stdc++.h>
using namespace std;

void solve() {
	;
}

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	#ifndef LOCAL
		freopen("cowtour.in", "r", stdin);
		freopen("cowtour.out", "w", stdout);
	#endif
	solve();
	return 0;
}